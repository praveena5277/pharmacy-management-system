# Server Settings
PORT=3000
JWT_SECRET=super_secret_pharmacy_token_key_12345

# Database Settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Praveena@46
DB_NAME=pharmacy_db
DB_PORT=3306

# Force mock database (in-memory) for testing
# If false, the server will try MySQL first and fallback to mock if MySQL connection fails
USE_MOCK_DB=false
